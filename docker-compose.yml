version: "3.8"
name: ${COMPOSE_PROJECT_NAME}

# Networks:
# - traefik-public: External network managed by standalone Traefik container
#   (created via init.sh or manually with: docker network create traefik-public)
# - app: Internal network for service-to-service communication (shared with standalone redis/minio)
networks:
  traefik-public:
    external: true
    name: traefik-public
  app:
    external: true
    name: ${COMPOSE_PROJECT_NAME}

services:
  api:
    container_name: ${COMPOSE_PROJECT_NAME}-api
    build:
      context: .
      dockerfile: apps/core-api/Dockerfile
      args:
        PROJECT_NAME: ${COMPOSE_PROJECT_NAME}
      # For using proxy on containers setup
      # network: host
      # args:
      #   HTTP_PROXY: http://127.0.0.1:2080
      #   HTTPS_PROXY: http://127.0.0.1:2080
      #   NO_PROXY: 127.0.0.1,localhost,::1
      #   http_proxy: http://127.0.0.1:2080
      #   https_proxy: http://127.0.0.1:2080
      #   no_proxy: 127.0.0.1,localhost,::1
    restart: always
    env_file:
      - .env
    networks:
      - traefik-public  # For external access via Traefik
      - app             # For internal communication with db, redis, minio
    depends_on:
      db:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik-public
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}-api.rule=Host(`${DOMAIN}`) && PathPrefix(`/api/v1`)
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}-api.priority=10
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}-api.entrypoints=websecure
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}-api.tls.certresolver=letsencrypt
      - traefik.http.services.${COMPOSE_PROJECT_NAME}-api.loadbalancer.server.port=4000

  db:
    image: postgres:15
    restart: always
    env_file:
      - .env
    networks:
      - app
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # ports:
    #   - "5432:5432"
    healthcheck:
      test: [ "CMD", "pg_isready", "-U", "postgres" ]
      interval: 30s
      timeout: 10s
      retries: 5

  pwa:
    container_name: ${COMPOSE_PROJECT_NAME}-pwa
    build:
      context: .
      dockerfile: apps/pwa/Dockerfile
      target: runner
      network: host
      args:
        PROJECT_NAME: ${COMPOSE_PROJECT_NAME}
        # For using proxy on containers setup
        # HTTP_PROXY: http://127.0.0.1:2080
        # HTTPS_PROXY: http://127.0.0.1:2080
        # NO_PROXY: 127.0.0.1,localhost,::1
        # http_proxy: http://127.0.0.1:2080
        # https_proxy: http://127.0.0.1:2080
        # no_proxy: 127.0.0.1,localhost,::1
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_CLARITY_PROJECT_ID: ${NEXT_PUBLIC_CLARITY_PROJECT_ID}
        NEXT_PUBLIC_ENABLE_MOCK_PAYMENTS: ${NEXT_PUBLIC_ENABLE_MOCK_PAYMENTS}
    restart: always
    env_file:
      - .env
    networks:
      - traefik-public  # For external access via Traefik
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    security_opt:
      - no-new-privileges:true
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik-public
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}-pwa.rule=Host(`${DOMAIN}`) || Header(`User-Agent`, `Clarity-Bot`)
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}-pwa.priority=1
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}-pwa.entrypoints=websecure
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}-pwa.tls.certresolver=letsencrypt
      - traefik.http.services.${COMPOSE_PROJECT_NAME}-pwa.loadbalancer.server.port=4000


volumes:
  postgres_data:
  browser_data:
